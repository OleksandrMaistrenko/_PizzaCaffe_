class ProductItem {
  constructor(id, imageUrl, title, description) {
    this.id = id;
    this.imageUrl = imageUrl;
    this.title = title;
    this.description = description;
  }
}

export default ProductItem;
